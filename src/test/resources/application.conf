{
  db {
    sql {
      driver      = com.mysql.jdbc.Driver
      database    = sql-test
      url         = "jdbc:mysql://localhost:3306/sql-test?useSSL=false"
      user        = "root"
      password    = ""
      maxThreads  = 5
    }
  }
}